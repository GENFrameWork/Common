services:

  notappexample:
    container_name: notappexample
    image: gen_prod:notappexample
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_notappexample
      args:
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}           
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./notappexample
    
    
  appbaseexample:
    container_name: appbaseexample
    image: gen_prod:appbaseexample
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_appbaseexample
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}                
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./appbaseexample
 

  memctrlexample:
    container_name: memctrlexample
    image: gen_prod:memctrlexample
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_memctrlexample
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./memctrlexample
 
 
  canvas2ddisplay:
    container_name: canvas2ddisplay
    image: gen_prod:canvas2ddisplay
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_canvas2ddisplay
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./canvas2ddisplay
   
  binconnpro:
    container_name: binconnpro
    image: gen_prod:binconnpro
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_binconnpro
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./binconnpro
    
  inibase:
    container_name: inibase
    image: gen_prod:inibase
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_inibase
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./inibase    
 
 
  netconn:
    container_name: netconn
    image: gen_prod:netconn
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_netconn
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./netconn
 
 
  databases:
    container_name: databases
    image: gen_prod:databases
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_databases
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./databases
 
 
  miniwebserver:
    container_name: miniwebserver
    image: gen_prod:miniwebserver
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_miniwebserver
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./miniwebserver
 
 
  scriptsexample:
    container_name: scriptsexample
    image: gen_prod:scriptsexample
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_scriptsexample
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-RELEASE}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./scriptsexample
 
 
  netcapture:
    container_name: netcapture
    image: gen_prod:netcapture
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_netcapture
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./netcapture
 
 
  canvas2d:
    container_name: canvas2d
    image: gen_prod:canvas2d
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_canvas2d
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./canvas2d
 
 
  ui_options:
    container_name: ui_options
    image: gen_prod:ui_options
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_ui_options
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./ui_options
 
 
  ui_message:
    container_name: ui_message
    image: gen_prod:ui_message
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_ui_message
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./ui_message
 
 
  devtestsconsole:
    container_name: devtestsconsole
    image: gen_prod:devtestsconsole
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_devtestsconsole
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./devtestsconsole
 
 
  devtestsdevices:
    container_name: devtestsdevices
    image: gen_prod:devtestsdevices
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_devtestsdevices
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./devtestsdevices
 
 
  devtestscanvas2d:
    container_name: devtestscanvas2d
    image: gen_prod:devtestscanvas2d
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_devtestscanvas2d
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./devtestscanvas2D
    
    
  xutils_unittests:
    container_name: xutils_unittest
    image: gen_prod:xutils_unittest
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_xutils_unittest
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./xutils_unit
       
       
  appupdatecreator:
    container_name: appupdatecreator
    image: gen_prod:appupdatecreator
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_appupdatecreator
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./appupdatecreator
    
    
  translatescan:
    container_name: translatescan
    image: gen_prod:translatescan
    tty: true
    networks:
      - example_app_net
    build:
      context: ../../
      dockerfile: Common/Docker/dockerfile_prod_translatescan
      args:       
        IMAGEBASE: ${IMAGEBASE:-}       
        TARGET: ${TARGET:-}
        TARGET_LOWERCASE: ${TARGET_LOWERCASE:-}        
        COMPILED_MODE: ${COMPILED_MODE:-}        
        APPPATH: ${APPPATH:-}
        APPNAME: ${APPNAME:-}
    command: ./translatescan

 
networks:

  example_app_net:
    driver: bridge
  
volumes:

  shared_mem: 
    driver: local
    
    
    